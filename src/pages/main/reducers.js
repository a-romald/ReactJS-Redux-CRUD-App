const initialState = {
      
};


function mainReducer(state = initialState, action) {
    switch (action.type) {
        default:
            return state;
    }
}

const MainReducer = {
    main: mainReducer
};

export default MainReducer;

